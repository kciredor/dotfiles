[general]
ui = ttyui
fsync = false
accounts = WeHandleIt
pythonfile = ~/.offlineimap/support.py

[Account WeHandleIt]
localrepository = WeHandleIt-Local
remoterepository = WeHandleIt-Remote
autorefresh = 5

[Repository WeHandleIt-Local]
type = Maildir
localfolders = ~/.maildir/wehandleit
nametrans = lambda folder: {'inbox':   'INBOX',
                            'drafts':  '[Gmail]/Concepten',
                            'sent':    '[Gmail]/Verzonden berichten',
                            'flagged': '[Gmail]/Met ster',
                            'spam':    '[Gmail]/Spam',
                            'trash':   '[Gmail]/Prullenbak',
                            'archive': '[Gmail]/Alle berichten',
                           }.get(folder, folder)

[Repository WeHandleIt-Remote]
type = Gmail
cert_fingerprint = 89091347184d41768bfc0da9fad94bfe882dd358
remoteusereval = whiUser()
remotepasseval = whiPass()
realdelete = yes
maxconnections = 3
keepconnectionopen = yes
keepalive = 60
idlefolders = ['INBOX', '[Gmail]/Verzonden berichten']
folderfilter = lambda folder: not re.search('(^[Gmail]/Belangrijk$|Mailbox)', folder)
nametrans = lambda folder: {'INBOX':                       'inbox',
                            '[Gmail]/Concepten':           'drafts',
                            '[Gmail]/Verzonden berichten': 'sent',
                            '[Gmail]/Met ster':            'flagged',
                            '[Gmail]/Spam':                'spam',
                            '[Gmail]/Prullenbak':          'trash',
                            '[Gmail]/Alle berichten':      'archive',
                           }.get(folder, folder)
