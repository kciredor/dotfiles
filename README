These are the config files I use daily.

Use git clone --recursive and run the deploy script.
(This will delete existing configs.)

Then install the vim bundles using Vundle's :BundleInstall.

Roderick Schaefer
