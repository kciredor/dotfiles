These are the config files I use daily.

Just clone the git repo somewhere and run the deploy script.
(This will delete existing configs)

Then install the vim bundles using Vundle

Roderick Schaefer
